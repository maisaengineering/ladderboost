<% if @professional_industry.errors.any? %>
$(".modal-body").html("<%= escape_javascript(render 'form',button_name: 'Save')  %>");

<% else %>

$('#myModal').modal('hide') ;

window.location = "/my_account";

<% end %>
